openapi: 3.0.3
info:
  title: Internal API Demo
  version: 1.0.0
paths:
  /logs:
    get:
      summary: List logs
      parameters:
        - name: X-API-Version # Legacy header - should trigger warning in internal-api profile
          in: header
          required: false
          schema:
            type: string
        - name: query
          in: query
          # Missing description - should trigger error (rule/parameter-description-required)
          schema:
            type: string
      responses:
        '200':
          description: OK
